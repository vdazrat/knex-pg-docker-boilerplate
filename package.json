{
  "name": "knex-docker-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "NODE_ENV='development' nodemon server/index.js --ignore node_modules/",
    "start": "NODE_ENV='production' nodemon server/index.js --ignore node_modules/",
    "knex-init": "NODE_PATH=./server/db && cd $NODE_PATH && knex init",
    "migrate:make": "NODE_PATH=./server/db && cd $NODE_PATH && knex migrate:make",
    "migrate:latest": "MODE='latest' node ./server/db/scripts/migrate.js",
    "migrate:rollback": "MODE='rollback' node ./server/db/scripts/migrate.js",
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^5.0.1",
    "express": "^4.16.3",
    "knex": "^0.14.4",
    "pg": "^7.4.1"
  }
}
